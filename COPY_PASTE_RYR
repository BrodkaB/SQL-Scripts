USE RLL;
GO

SELECT TOP (20)
    ID,
    StartTime,
    EndTime,
    DatabaseName,
    SchemaName,
    ObjectName,
    IndexName,
    CommandType,
    Command,
    ErrorNumber,
    ErrorMessage
FROM dbo.CommandLog
WHERE ObjectName = 'D996M'
ORDER BY ID DESC;
