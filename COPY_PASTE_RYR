SELECT
    OBJECT_NAME(object_id) AS table_name,
    forwarded_record_count,
    page_count,
    record_count
FROM sys.dm_db_index_physical_stats
(
    DB_ID(),
    OBJECT_ID('dbo.D996M'),
    0,
    NULL,
    'DETAILED'
)
WHERE index_id = 0;
