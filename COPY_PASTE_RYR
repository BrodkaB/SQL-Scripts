SELECT
    USER_NAME(dp.grantee_principal_id) AS Principal,
    dp.state_desc,
    dp.permission_name,
    OBJECT_NAME(dp.major_id) AS ObjectName
FROM sys.database_permissions dp
WHERE USER_NAME(dp.grantee_principal_id) = 'UserName';

EXEC sp_helprotect @username = 'UserName';
